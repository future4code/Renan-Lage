/**
 * EXEMPLO DE UTILIZAÇÃO DA 'comprarCarta'
 * 
 * 
    const carta = comprarCarta(); // Sorteia uma carta. Por exemplo, o rei de ouros
    
    console.log(carta.texto) // imprime o texto da carta. Exemplo: "K♦️" (indica "K" de ouros)
    console.log(carta.valor) // imprime o valor da carta (um número). Exemplo: 10 (dado que "K" vale 10)
 * 
 * 
 * 
 * 
 */
    
    
/*
console.log("Boas vindas ao Black Jack!");

const jogador = [];
const computador = [];

if(confirm("Quer começar uma rodada?")){
   // layout jogador
   jogador.push(comprarCarta())
   jogador.push(comprarCarta())
   pontuacaoJogador = jogador[0].valor + jogador[1].valor;
   console.log(`Usuário - cartas: ${jogador[0].texto} ${jogador[1].texto}  - pontuação ${pontuacaoJogador}`)

   // layout computador
   computador.push(comprarCarta())
   computador.push(comprarCarta())
   pontuacaoComputador = computador[0].valor + computador[1].valor;
   console.log(`Computador - cartas: ${computador[0].texto} ${computador[1].texto}  - pontuação ${pontuacaoComputador}`)

   if(pontuacaoComputador > pontuacaoJogador){
      console.log("O computador ganhou!")
   }
   if(pontuacaoJogador > pontuacaoComputador){
      console.log("O usuário ganhou!")
   }
   if(pontuacaoComputador == pontuacaoJogador){
      console.log("Empate!")
   }

}else{
     console.log("O jogo acabou.");
}
*/